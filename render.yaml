services:
  - type: web
    name: necoma
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn snrproject.asgi:application -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
